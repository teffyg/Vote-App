var express = require('express');
var router = express.Router();

/* GET home page. */
// router.get('/', function(req, res, next) {
//   res.render('index', { title: 'Express' });
// });

//ruta para la vista de candidatos
// le decimos que use el componente de los candidatos
//  router.get('/candidates-list', function(req, res, next) {
//  res.render('index')
//  });
module.exports = router;
